## DeepLung 前端（新版）

该目录已替换为你提供的完整前端设计版本（单应用，包含医生与患者相关界面流程）。

## 启动项目

在项目根目录执行以下命令安装依赖：

```bash
npm install
```

启动前端开发服务器：

```bash
npm run dev
```

启动后默认访问地址：

```text
http://127.0.0.1:5173/
```

如果需要让局域网内其他设备访问，可以使用：

```bash
npm run dev
```
